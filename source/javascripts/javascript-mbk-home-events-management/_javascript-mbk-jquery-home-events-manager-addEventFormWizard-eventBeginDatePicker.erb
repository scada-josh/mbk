

$('#eventBeginDate').datepicker({
    // autoclose: true,
    // format: "yyyy-mm-dd",
    // todayHighlight: true,
    // todayBtn: "linked",
    // yearOffset:543,  // ใช้ปี พ.ศ. บวก 543 เพิ่มเข้าไปในปี ค.ศ
    // language: "th"
    autoclose: true,
    // format: "yyyy-mm-dd",
    format: "dd/mm/yyyy",
    todayHighlight: true,
    todayBtn: "linked"
});

// $('#eventBeginDate').datepicker().on('changeDate', function(e) {
//         // `e` here contains the extra attributes
//         console.log(e);
//         console.log(e.timeStamp);
//         //console.log(moment().format());
//         console.log(moment(e.timeStamp).format('YYYY-MM-DD'));
//         console.log(moment().unix(e.timeStamp).format('YYYY-MM-DD'));
// });